# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets
.env
.env.*
!.env.example

# Build artifacts & archives (không commit)
# Các thư mục build cũ từng nhúng khoá JWT đã bị vô hiệu hoá 28/07/2026 và cả mã
# route /tv đã gỡ. Kéo-thả nhầm một trong số này lên Netlify là app hỏng ngay
# (không ai đăng nhập được) và lỗ hổng /tv quay lại. Chỉ giữ đúng `dist/`.
deploy-moi
deploy-netlify/
dist-SAO-LUU-*/
deploy.zip
qlsx-deploy-moi.zip
*.zip

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Anh goc avatar (ban nen da o public/avatars/)
/avatar/
seed_kpi_*.sql
KPI/

seed_cham_cong_*.sql
