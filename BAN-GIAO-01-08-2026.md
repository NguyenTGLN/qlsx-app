# Bàn giao — 01/08/2026

> Việc: **điểm KPI cải tiến chỉ lấy từ bài cải tiến đã duyệt, không còn chấm tay ở Bảng chấm chung.**
> Nhánh: `kpi-cai-tien-go-khoi-bang-chung` · 7 commit · Spec + kế hoạch nằm ở `docs/superpowers/`

---

## 1. Hoá ra vấn đề không phải "điểm lấy sai nguồn"

Luật chấm tự động **đã có sẵn và vẫn chạy đúng từ trước**: `luatDongGopCaiTien`
(`src/lib/kpiTuDong.js:403`) đếm bài `DA_DUYET` theo mốc **duyệt** (`reviewed_at`), tối
thiểu 2 bài/tháng, và **đè lên** điểm chấm tay mỗi lần mở màn hình.

Cái hỏng là **màn hình vẫn mời người ta chấm tay một con số sẽ bị vứt đi**:

| Đo được trên DB | Kết quả |
|---|---|
| `DONG_GOP_CAI_TIEN` kỳ 2026-07 | `cham_chung = true` → nằm trong Bảng chấm chung, 13 ô nhập **mở** |
| `KpiBangChung.jsx` | **Không hề kiểm `cach_cham`** — khác `KpiTab.jsx:1064` vốn có kiểm và khoá |
| Dấu vết để lại | 13/13 dòng mang `chot_boi = 'Nguyên'` — đã có người chấm tay thật, và số đó đã bị vứt |

Nói cách khác: bác đã ngồi chấm 13 ô, app ghi xuống CSDL thật, rồi lặng lẽ đè lại thành 0.
Không có thông báo nào.

## 2. Đã sửa gì

**Dữ liệu** — `sql/go_cai_tien_khoi_bang_chung.sql` (đã chạy lúc 08:2x ngày 01/08):
`cham_chung = false` cho `DONG_GOP_CAI_TIEN` ở **mọi kỳ**. Phải mọi kỳ, vì hàm `tao_ky_kpi`
copy cột này sang kỳ mới — chặn một kỳ thì tháng sau nó quay lại mà không báo lỗi gì.

**Mã nguồn** — khoá theo `cach_cham`, áp cho **mọi** chỉ tiêu tự động chứ không hard-code
riêng cải tiến:
- Ô nhập của chỉ tiêu tự động → **chỉ đọc**, nền tím, có chữ "tự động" và chú thích.
- Chặn ghi đặt ở `luu()` — chỗ **mọi** đường ghi đi qua, không phải chỉ ở `roiO`.
- Popup "＋ Thêm chỉ tiêu" **không còn liệt kê** chỉ tiêu tự động → không thêm nhầm lại được.

## 3. Bác sẽ thấy khác đi thế nào

- **Bảng chấm chung kỳ 07/2026: 5 dòng còn 4.** ĐÓNG GÓP CẢI TIẾN biến mất khỏi màn hình đó.
- **Điểm KPI của 13 người: KHÔNG đổi** — vốn đã là 0 vì luật tự động đã đè sẵn từ trước.
- Popup "＋ Thêm chỉ tiêu" **ngắn đi 6 mục**: ngoài ĐÓNG GÓP CẢI TIẾN còn mất BÁO CÁO KẾT
  QUẢ CÔNG VIỆC, CHUYÊN CẦN BỘ PHẬN, HOÀN THÀNH CÔNG VIỆC ĐÚNG HẠN, SẢN XUẤT, VIDEO KỸ
  THUẬT. Cả 6 đều là chỉ tiêu app tự tính; đưa bất kỳ cái nào vào bảng chung đều dính đúng
  cái bẫy vừa gỡ. **Đây là thay đổi rộng hơn một chỉ tiêu — nói trước để bác không bất ngờ.**
- 4 chỉ tiêu chấm tay (5S, CHẤM KPI, QUY ĐỊNH CÔNG TY, VĂN HOÁ CÔNG TY) **giữ nguyên** —
  đã đo cả 4 là `THU_CONG`, và có test khoá lại chuyện này.

## 4. ⚠️ Việc bác cần làm

### Tháng 7 cả công ty đang 0 điểm chỉ tiêu này

Bảng `cai_tien` hiện có **đúng 1 bài**, tựa "LÀM SẠCH KHÔNG QUAN XUNG QUANH NƠI LÀM VIỆC",
trạng thái **CHỜ DUYỆT**, chưa duyệt.

Muốn có điểm thì **duyệt bài trong tab Cải tiến** — không phải chấm tay. Duyệt xong điểm
**tự cộng**, không phải làm gì thêm. Chỉ tiêu là 2 bài/tháng: 1 bài = 50%, 2 bài = đủ điểm.

### Kiểm bằng mắt — phần tôi KHÔNG làm được

App chạy sạch (build ổn, không lỗi console lẫn lỗi server), nhưng tôi **dừng ở màn hình
đăng nhập** và không nhập mật khẩu thay bác. Nên phần nhìn tận mắt là của bác:

1. **Công việc → KPI → kỳ 2026-07 → Bảng chấm chung** — phải còn **4 dòng**, dòng chữ đầu
   màn hình đọc "4 chỉ tiêu × 13 nhân viên".
2. Gõ thử một số vào ô **5S** rồi tab ra — **phải lưu bình thường**, chấm thiếu vẫn hiện
   nút "＋ ghi lý do". Đây là luồng tôi lo nhất và là thứ đáng kiểm nhất.
3. Mở bảng KPI một nhân viên bất kỳ — ĐÓNG GÓP CẢI TIẾN vẫn còn, nền **tím**, **0/2 điểm**,
   ghi chú nói "chưa có cải tiến nào được duyệt… Còn 1 bài đang chờ duyệt/bổ sung".

Nếu điểm 2 sai, dừng lại báo tôi ngay — đó là dấu hiệu khoá nhầm sang chỉ tiêu chấm tay.

## 5. Kiểm tra bảo mật

### Đã ĐÓNG được lỗ hổng kiểm chứng của hôm qua

Bàn giao 31/07 ghi: *"Phần CHƯA kiểm chứng được: đường **ghi** bằng khoá công khai… đó là
**suy ra, không phải đo**."* **Hôm nay đo được rồi.** Dùng khoá `sb_publishable_…` lấy thẳng
từ mã nguồn, trên **dòng có thật**:

| Phép thử | Kết quả |
|---|---|
| ĐỌC `kpi_chi_tieu`, `cai_tien`, `kpi_nhat_ky`, `nhan_vien`, `app_users`, `cham_cong` | **0 dòng** cả 6 |
| ĐỌC cả 4 view (`thong_ke_ktv`, `v_tien_do`, `view_thong_ke_ban_hang`, `sales_90d_summary`) | **0 dòng** |
| SỬA `kpi_chi_tieu` (13 dòng có thật) | `[]` — chặn |
| SỬA `cai_tien` (dòng id=1 có thật) | `[]` — chặn |
| XOÁ `kpi_chi_tieu` (dòng thử có thật, tôi tự tạo rồi tự dọn) | `[]` — chặn |
| CHÈN `kpi_chi_tieu` / `cai_tien` | lỗi `42501` vi phạm RLS |
| CHÈN bài cải tiến giả trạng thái `DA_DUYET` | lỗi `42501` — **không ăn gian điểm KPI được** |

Sau mỗi phép thử đã **kiểm lại trong CSDL**, không tin `[]` suông: `chot_boi` vẫn đủ 13 dòng
**có dấu** (phép thử cố ghi đè bản không dấu), 0 dòng bị sửa, 0 dòng lạ chèn vào.

Phép cuối quan trọng nhất: điểm KPI cải tiến giờ **phụ thuộc hoàn toàn** vào bài `DA_DUYET`
trong bảng `cai_tien`. Người ngoài chèn được một bài giả là thổi được điểm cho bất kỳ ai.
Đã đo: **chặn**.

### Các mặt khác

- **Storage** — 3 bucket riêng tư (`cccd` 408 tệp, `qc_images` 329, `task-attachments` 14):
  liệt kê trả `[]`, và với **một tệp CCCD có thật** thì tự ký link → 404, tải công khai →
  400, tải bằng khoá công khai → 400.
- **RPC** — không hàm `SECURITY DEFINER` nào chạy được bằng `anon` có đụng `kpi_chi_tieu`
  hay `cai_tien`.
- **View** — cả 4 đều có `security_invoker = true`, nên chúng thừa hưởng RLS thay vì bỏ qua.
- Thay đổi này **không sửa** RLS, policy, Storage, khoá API hay luồng đăng nhập nào.

### Phần CHƯA kiểm chứng được — nói thẳng

1. **Kiểm bằng trình duyệt thật.** Skill yêu cầu, vì app là JavaScript nên `curl` không chạy
   được giao diện. Tôi vướng màn hình đăng nhập và không nhập mật khẩu thay bác. Mục 4 ở
   trên là danh sách để bác tự chốt.
2. **Không rà lại toàn bộ mặt RPC công khai cũ** (`dang_nhap`, `xac_nhan_bao_hanh`,
   `xem_bien_ban_*`…). Đó là cửa vào công khai có chủ đích, có từ trước, và **không liên
   quan tới thay đổi này** — nhưng cũng có nghĩa là tôi không khẳng định gì về chúng hôm nay.

## 6. Ghi chú kỹ thuật để lần sau khỏi mò lại

- **Vì sao KHÔNG xoá `diem_chot = 0` và `chot_boi`** dù chúng là tàn dư chấm tay: khi bảng
  `cai_tien` lỗi tải, `KpiTab` đặt `caiTien = null` → luật trả "không chấm" → rơi về
  `diem_chot` trong CSDL. Số `0` là giá trị an toàn. Đặt `null` thì engine coi là **đạt đủ
  điểm** — tức một lỗi mạng hoá thành 2/2 điểm cho cả 13 người.
- **Hai nguồn sự thật cho "chấm tự động"**: cột `cach_cham` trong CSDL (thứ khoá ô nhập) và
  bảng `LUAT_TU_DONG` trong `kpiTuDong.js` (thứ thật sự tính điểm). Chúng khớp nhau hôm nay,
  nhưng **thêm luật vào `LUAT_TU_DONG` mà quên đặt `cach_cham = 'TU_DONG'` trong CSDL thì ô
  nhập mở lại, không có lỗi nào báo.** Đã ghi cảnh báo ngay tại `laChamTuDong`.
- **Có một ngoại lệ cố ý**: `CHUYEN_CAN_CA_NHAN` có luật tự động nhưng đặt `nhuongChamTay` —
  điểm chốt tay của người thật thắng điểm tự động. Nên nó phải **giữ** `THU_CONG` và **không**
  bị khoá. Khoá nó là làm hỏng một luồng đang chạy đúng.
- **`dsChiTieuCoSan` cố ý KHÔNG lọc chỉ tiêu tự động.** Nó phục vụ form "Thêm chỉ tiêu cho
  một nhân viên" ở tab KPI — lọc ở đó thì **nhân viên mới không được cấp chỉ tiêu cải tiến
  nữa**, app không có dòng để chấm và điểm biến mất.
- File SQL có **phần hoàn tác dán chạy được ngay** ở cuối. Lưu ý hoàn tác chỉ làm chỉ tiêu
  **hiện lại** trong bảng chung; ô nhập vẫn khoá vì phần khoá nằm ở mã nguồn.

## 7. Deploy

Có sửa mã giao diện ⇒ **phải deploy lại**. Kéo-thả thư mục `dist/` lên Netlify, **không
build trên Netlify**. Đừng kéo nhầm các thư mục build cũ (`deploy-moi`, `dist-SAO-LUU-*`,
`deploy-netlify`) — chúng nhúng khoá JWT đã bị vô hiệu hoá.

Nhánh `kpi-cai-tien-go-khoi-bang-chung` **chưa gộp vào `master`**. SQL thì đã chạy rồi, nên
tới khi deploy bản mới sẽ có một khoảng chỉ tiêu đã biến khỏi bảng chung mà mã cũ vẫn chạy —
vô hại, vì mã cũ chỉ đọc `cham_chung` để quyết định hiện hay không.
