# Bàn giao — 31/07/2026

Chủ đề: luồng Zalo → Supabase (KPI CSKH + tự động đóng việc báo cáo cuối ngày).

---

## 1. Đã sửa xong — trigger tự động đóng việc báo cáo

**Triệu chứng:** nhân viên gửi báo cáo lên nhóm Zalo nhưng việc "Báo cáo công việc cuối
ngày" trong tab Công việc được giao không tự chuyển sang hoàn thành. Lần cuối trigger
thật sự chạy được là **26/05/2026** — nằm im hơn hai tháng mà không ai biết.

**Hai lỗi chồng nhau, đã sửa cả hai** trong `sql/setup_auto_complete_task.sql` và đã áp
dụng vào CSDL thật:

| | Trước | Sau |
|---|---|---|
| Mã nhóm | `6274675927160413910` — **sai nhóm** | `6491009630666576664` (nhóm đang dùng), giữ thêm mã cũ |
| Từ khoá | chỉ `em gửi báo cáo` / `em báo cáo` | có `báo cáo` (kể cả viết liền) **hoặc** từ `bc` đứng riêng |

Mã nhóm đúng được xác nhận bằng tin thật lúc 19:00 ngày 31/07/2026. Nhóm cũ
`6274675927160413910` chỉ nhận đúng 2 tin (ngày 06/06/2026) rồi thôi — giữ lại trong
danh sách cho an toàn, không gây hại gì.

Từ khoá cũ quá chặt: tin báo cáo thật ngày 06/06 là *"E gửi bc ngày 06.06 ạ"* — trượt
hoàn toàn.

### Luật đang chạy — 5 điều kiện phải đúng ĐỒNG THỜI

Trigger `trigger_zalo_auto_complete_task` chạy **AFTER INSERT trên `zalo_messages`**:

1. **Đúng nhóm** — `thread_id` ∈ {`6491009630666576664`, `6274675927160413910`}
2. **Nội dung khớp** — `content ILIKE '%báo cáo%'` OR `ILIKE '%báocáo%'` OR `~* '\ybc\y'`
   (regex `\y` để "bc" phải đứng riêng thành từ, không bắt nhầm `ABC123`)
3. **Người gửi có trong `nhan_vien`** — tra theo `uid_from`. Không thấy ⇒ dừng, im lặng.
   *Luật này tra thẳng bảng `nhan_vien`, KHÔNG đọc cột `is_staff` của tin nhắn.*
4. **Có việc đang mở đúng ngày** — `cong_viec_duoc_giao` có `title` chứa "Báo cáo công
   việc cuối ngày", `status = 'IN_PROGRESS'`, hạn (`due_date`, thiếu thì `created_date`)
   rơi đúng hôm nay giờ VN, và người gửi nằm trong `assignee_ids` (việc nhóm: ai gửi
   cũng đóng cho cả nhóm)
5. **Nhiều việc cùng khớp** ⇒ chỉ đóng một, lấy việc tạo gần nhất

Đủ 5 điều kiện thì ghi hai thứ: `cong_viec_duoc_giao` chuyển `COMPLETED` +
`completed_date` + `updated_by`; và thêm dòng `tien_do` mã `TD-xxx` nội dung *"Hệ thống
tự động ghi nhận HOÀN THÀNH do [tên] đã gửi báo cáo lên nhóm Zalo."*

**Dòng log này là cách phân biệt đóng tự động với bấm tay** — bấm tay không sinh log.
Dùng đúng dấu hiệu này để đo "lần cuối trigger chạy".

Thiếu điều kiện nào thì **im lặng bỏ qua**, không báo lỗi. Khi thấy không ăn, loại trừ
lần lượt: sai nhóm? nội dung không khớp? chưa map UID? không có việc mở hôm nay?

Gửi tin thứ hai không đóng thêm lần nữa (điều kiện 4 đòi `IN_PROGRESS`), nên không lo log trùng.

### Đã kiểm chứng thế nào

Chạy trong **giao dịch tự huỷ** — chèn tin thử, kiểm tra, rồi `RAISE` để rollback, không
để lại dòng nào trong `zalo_messages`. Đã xác nhận không sót rác sau khi chạy.

| Ca | Kỳ vọng | Kết quả |
|---|---|---|
| Đúng nhóm, nội dung `"3"` | không đóng | ✔ |
| Có "báo cáo" nhưng gửi nhóm khác | không đóng | ✔ |
| Đúng nhóm + `"E gửi bc ngày 31.07 ạ"` | đóng việc + ghi log | ✔ |

Luật nhận diện còn chạy qua 13 câu thật (gồm `BÁO CÁO NGÀY 31/07`, `bc`, và các ca phải
trượt như `ABC123`, `abcxyz`, `ok em nhận`) — đúng cả 13.

### Giới hạn đã biết

- **Ảnh chụp báo cáo không kèm chú thích sẽ trượt** — lúc đó `content` chỉ là `[Hình ảnh]`.
  Đây là kiểu gửi khá phổ biến, cần theo dõi vài hôm.
- Câu kiểu *"em xem báo cáo rồi nhé"* vẫn bị tính là đã báo cáo.
- **Nút thắt lớn nhất: chỉ 3/17 nhân viên có `uid_from`** (`admin`/Nguyên,
  `hhx`/Xuyên, `nttd`/Dương). 14 người còn lại gửi báo cáo đúng kiểu vẫn không được ghi nhận.

---

## 2. Chẩn đoán — tab KPI CSKH Zalo "không cập nhật dữ liệu mới"

**Kết luận: tab không hỏng.** Nó hiển thị đúng 100% những gì có trong CSDL — đo được
10.623 hội thoại trên màn hình, đúng bằng số dòng có `first_message_ts` tính đến hết
28/07. Trigger, realtime, quyền đọc, bộ lọc ngày đều bình thường.

**Nguyên nhân thật: nguồn tin Zalo chết 69 giờ.**

| Điểm đo | Kết quả |
|---|---|
| Tin cuối trước khi đứt | 28/07 17:23:42 |
| Tin kế tiếp | 31/07 14:24:05 |
| Nhịp bình thường | 14–32 tin/giờ trong giờ hành chính |
| Sau khi sống lại | 55 tin trong khoảng 14:27 → 18:05 ngày 31/07 |

**Dữ liệu Zalo ngày 29 và 30/07 mất hẳn** — webhook không lưu ở đâu khác, không lấy lại được.

Trong file workflow n8n không có gì gây ra được cú đứt đó (logic không đổi, không nhóm
liên quan nào bị chặn). Nhiều khả năng **phiên đăng nhập Zalo của node
`n8n-nodes-ultimate.zaloTrigger` hết hạn** — node cộng đồng này giữ session Zalo, session
chết thì workflow vẫn hiện "Active" nhưng không nhận được sự kiện nào. **Đây là suy đoán,
chưa đo được** vì không truy cập được n8n. Dấu hiệu nhận biết nếu tái diễn: workflow vẫn
Active mà execution log trống trơn.

---

## 3. Bản đồ luồng Zalo — để lần sau khỏi mò lại

```
Zalo  →  n8n "Zalo Trigger"  →  If1  →  If  →  INSERT zalo_messages
                                                      │
                          ┌───────────────────────────┴───────────────────────────┐
                          │ BEFORE INSERT                        AFTER INSERT     │
                          │ process_zalo_message_kpi     process_zalo_auto_complete_task
                          │ → dựng zalo_conversations    → đóng việc báo cáo cuối ngày
                          │   (nuôi tab KPI CSKH)          (ghi cong_viec_duoc_giao + tien_do)
                          └────────────────────────────────────────────────────────┘
```

### Những bẫy đã đo được trong cấu hình n8n

**Danh sách `ignoreThreads` (9 nhóm, một mã bị lặp 2 lần):** Chương trình Khuyến mại và
bán hàng, THAY ĐỔI ĐLĐ, CÔNG VIỆC KHO, Thư Viện TGLN Video-Content 4, SALE - KẾ TOÁN
EUROMADE, Kho Euromade, cộng 3 mã chưa từng có tin nào.

Cả 6 nhóm nhận diện được đều **tắt đúng ngày 04/06/2026** — đó là dấu vân tay của việc
bật danh sách chặn. Nhóm nào chết cùng một ngày như vậy thì nghi ngay `ignoreThreads`.

**Node `If` gán `is_staff` là vô tác dụng.** CSDL ghi đè trường này: trigger
`process_zalo_message_kpi` tự tính lại `is_staff` từ `nhan_vien.uid_from`. Giá trị n8n
gửi lên bị vứt bỏ hoàn toàn. Tệ hơn, cả 4 UID trong node `If` đều **không có** trong
`nhan_vien`, và 2 trong số đó (`4789366080551004341`, `698042764074105709`) chưa từng gửi
tin nào trong hơn 23.000 tin — sai hoặc đã chết.

**Hệ quả: nhân viên chưa map UID bị CSDL tính là khách hàng**, làm phồng "Tổng lượt hội
thoại" và sai "Tốc độ phản hồi (TB)" trong tab KPI. Ví dụ đo được: "Ngọc"
(`1650978188571730661`, 96 tin) hiện ở cột Khách hàng với câu trao đổi nội bộ *"có khách
hàng cần sữa máy / có ai nhận đơn không"*.

**Nhiều nhân viên có 2 tài khoản Zalo.** Phong có `9002555586731314915` (đang dùng) và
`8209801293158810413` (ngưng từ 08/06). Nguyễn Duyên có `8313230541287993426` (đang dùng)
và `9105739124680441244`. Node `If` của n8n đang trỏ vào tài khoản **cũ**.

### Workflow "lấy ID nhóm" — vì sao báo lỗi 400

Node HTTP Request ghi vào `zalo_groups` trả `PGRST102 – Empty or invalid json`.
Là **400 chứ không phải 401** ⇒ khoá vẫn sống, hỏng thuần ở phần thân yêu cầu.

Nguyên nhân: `contentType: "raw"` bắt thân yêu cầu phải là **chuỗi**, nhưng biểu thức
`={{ { group_id: …, group_name: … } }}` trả về **object** ⇒ n8n ép thành chữ
`[object Object]` ⇒ PostgREST không đọc được.

Sửa: đổi Body Content Type sang **JSON**, chọn *Using Fields Below*, khai hai trường
`group_id` và `group_name`. Hoặc giữ `raw` và bọc `JSON.stringify(...)`.

Phần `Prefer: resolution=merge-duplicates` thì ổn — `group_id` là khoá chính
(`zalo_groups_pkey`), upsert chạy lại nhiều lần không sinh trùng.

Hai bẫy sẽ lộ ra ngay sau khi sửa xong:
- `gridVerMap` là map `{ "<mã nhóm>": <số phiên bản> }`. Node lấy thông tin nhóm cần
  **khoá** (mã nhóm), nếu `Split Out` nhả ra **giá trị** (số phiên bản) thì tham số sai.
- `group_id` và `group_name` đều `NOT NULL`. Thấy lỗi *"null value violates not-null"*
  nghĩa là đường dẫn trường sai, không còn là lỗi JSON nữa.

---

## 4. ⚠️ Việc bác cần làm

### Gấp nhất — thu hồi khoá secret đã lộ

Khoá `sb_secret_…` đang **gắn cứng trong header** của node HTTP Request thuộc workflow
"lấy ID nhóm", và đã bị dán ra ngoài n8n trong lúc trao đổi. Khoá này **bỏ qua toàn bộ
RLS** — ai cầm nó đọc/sửa/xoá được mọi bảng.

Supabase Dashboard → Settings → API Keys → thu hồi khoá cũ, tạo khoá mới, cập nhật lại
trong n8n (cả credential lẫn mọi node HTTP Request đang gắn cứng).

**Lần sau xuất workflow để gửi đi, xoá giá trị khoá trong `headerParameters` trước.**

### Bảng `zalo_groups` đang trống — 90 dòng đã bị xoá

Thống kê Postgres: 90 dòng bị xoá, còn 0, autovacuum dọn lúc 18:43 ngày 31/07. Lúc 18:27
bảng còn tra ra tên nhóm bình thường.

Hậu quả đang có hiệu lực:
- **Bộ lọc "Nhóm Zalo" trong tab KPI rỗng** — dropdown đọc thẳng bảng này
- **Hội thoại nhóm mới mất tên nhóm** — tab hiện `ID: <số>` thay vì tên
- Hội thoại cũ không sao: 79/104 nhóm còn tên lưu sẵn trong `zalo_conversations`

Dựng lại được từ chính `zalo_conversations` (79 nhóm có tên) — chưa làm, chờ xác nhận.

### Điền `uid_from` cho nhân viên — nút thắt của cả hai luồng

Chỉ 3/17 người có UID. Khớp theo tên là bất khả thi (riêng "Hà" ra hơn 50 ứng viên, gần
hết là khách hàng và đại lý). Dưới đây là những trường hợp có **căn cứ ngoài tên**, cần
xác nhận từng dòng trước khi ghi:

| UID | Tên Zalo | Số tin | Ứng viên | Căn cứ |
|---|---|---|---|---|
| `1650978188571730661` | Ngọc | 96 | `nbn` / Ngọc | n8n xếp vào nhóm bỏ-tin-cá-nhân ⇒ đã coi là nội bộ |
| `9002555586731314915` | Kỹ Thuật TGLN - Phong | 54 | `ndp` / Phong | như trên + tên gắn thương hiệu công ty |
| `8313230541287993426` | Nguyễn Duyên | 40 | `nv8` / Duyên | như trên; còn gửi tin ngày 31/07 |
| `715086275848796206` | Nguyen | 53 | ? | node `If` của n8n gán `is_staff = true` |
| `8209801293158810413` | Kỹ Thuật TGLN - Phong | 4 | `ndp` / Phong (tài khoản **cũ**) | node `If`; ngưng từ 08/06 |
| `9105739124680441244` | Nguyễn Duyên | 2 | `nv8` / Duyên (tài khoản **cũ**) | chính là người gửi báo cáo 06/06 |

Còn Xuân, Hằng, Bích, Hà, Vân Anh, Hĩu, Thiện, Thơ, Tuấn, Anh Giang thì không có căn cứ
nào để đoán. **Cách chắc nhất: chờ họ gửi báo cáo vào nhóm `6491009630666576664`**, UID
hiện ra trong `zalo_messages`, lúc đó map chính xác tuyệt đối.

> ⚠️ Điền `uid_from` **cũng đổi tab KPI CSKH**: những người này thôi bị đếm là khách
> hàng, nên "Tổng lượt hội thoại" và "Tốc độ phản hồi" sẽ khác số cũ, và chỉ áp dụng cho
> tin nhắn **mới** — số liệu cũ giữ nguyên.

---

## 5. Kiểm tra bảo mật

**Đo được bằng chính khoá công khai** (khoá `sb_publishable_…` nằm sẵn trong `dist`,
Ctrl+U là thấy): đọc trả về **0 dòng** trên cả `zalo_messages`, `cong_viec_duoc_giao`,
`tien_do`, `zalo_groups`, `nhan_vien`.

Ngoài ra:
- Thay đổi hôm nay **không tạo** view / materialized view mới
- 4 view sẵn có vẫn giữ `security_invoker = true` (bản vá 28/07 còn nguyên)
- Hàm `process_zalo_auto_complete_task` là **SECURITY INVOKER** và trả về kiểu `trigger`
  ⇒ PostgREST không gọi được như RPC. Không mở thêm đường leo quyền nào.
- `anon` **không có policy nào** trên 3 bảng liên quan (chỉ `chi_nguoi_dang_nhap` cho
  `authenticated`, áp cho ALL)

**Phần CHƯA kiểm chứng được:** đường **ghi** (sửa/xoá/chèn) bằng khoá công khai. Môi
trường làm việc chặn mọi lệnh ghi ra API sản xuất. Ở tầng CSDL thì `anon` không có policy
— cùng cơ chế đã chặn thành công đường đọc — nhưng **đó là suy ra, không phải đo**.

Lệnh để tự chốt (lấy khoá `sb_publishable_…` trong `dist`, thay vào `<KEY>`):

```bash
curl -s -X PATCH "https://ngwkzicrnspeggunsblr.supabase.co/rest/v1/cong_viec_duoc_giao?id=eq.CV-927" -H "apikey: <KEY>" -H "Authorization: Bearer <KEY>" -H "Content-Type: application/json" -H "Prefer: return=representation" -d '{"status":"KIEM_THU"}'
```

Trả về `[]` là đạt. Trả về dữ liệu dòng là thủng.

---

## 6. Deploy

**Thay đổi hôm nay là phía CSDL, không đụng mã giao diện** — về nguyên tắc **không cần
deploy lại**. Thư mục `dist/` vẫn được build mới để bác có bản chạy được ngay nếu cần.

Cách deploy giữ nguyên như cũ: kéo-thả thư mục `dist/` lên Netlify, **không build trên
Netlify**. Tuyệt đối không kéo nhầm các thư mục build cũ (`deploy-moi`,
`dist-SAO-LUU-*`, `deploy-netlify`) — chúng nhúng khoá JWT đã bị vô hiệu hoá và mã route
`/tv` đã gỡ; kéo nhầm là app hỏng ngay và lỗ hổng cũ quay lại.
