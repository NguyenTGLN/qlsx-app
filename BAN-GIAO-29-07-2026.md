# Bàn giao — 29/07/2026

## Đã làm xong hôm nay

### 1. Sửa lỗi BOM khai ngược chiều (đã áp dụng vào dữ liệu thật)

Ba dòng trong `bom_items` khai linh kiện nhỏ "chứa" nguyên một máy lọc nước nóng lạnh:
`WASHER-LT` → `WT-028S-RO` ×4, `V-FR-300` → ×1, `E-V-SV-D-LM` → ×1. App nổ BOM cho
18.705 máy trong khi nhu cầu thật là 475.

Đã xoá. Tổng SL đề xuất từ **2.327.906 xuống 170.320**. Sao lưu ở
`bak_bom_items_20260729` và `bak_purchase_proposals_20260729` (RLS bật + REVOKE, đã đo
bằng khoá công khai: không đọc/sửa/xoá được). Tệp SQL kèm phần hoàn tác:
`sql/fix_bom_dao_chieu_20260729.sql`.

### 2. Engine MRP mới — Giai đoạn 1 xong

Nhánh `feat/de-xuat-theo-dot`. Tệp mới `src/lib/mrp.js` + `src/lib/mrp.test.js`.
**40 test, 746 test toàn dự án xanh, eslint sạch.**

`git diff` trên `src/pages/`, `dksxEngine.js`, `proposalQty.js` là **rỗng** — app đang
chạy không bị đụng dòng nào. Bác dùng bình thường.

Bốn hàm: công thức tồn an toàn (`lead×2 + an toàn`), xếp tô-pô BOM có bắt vòng lặp,
nổ BOM trừ tồn từng cấp, dựng dòng đề xuất kèm vết tính toán.

Đối chiếu trên dữ liệu thật: **100 dòng / 30.752 đơn vị**, so với 138 dòng / 170.320
hiện tại. Chi tiết ở `docs/superpowers/plans/2026-07-29-mrp-doi-chieu-du-lieu-that.md`.

---

## ⚠️ Việc bác cần làm

### Gấp — bảng đề xuất đang lệch

Nhu cầu ECO-100RO đã hạ về 259 nhưng bảng đề xuất chưa tính lại, vẫn giữ số theo 300 máy.
Q-5044 đang hiện **21.749**, đúng phải là **21.421**.

> Tab **Tồn HH** → chọn `ECO-100RO` → xoá số điền sẵn, gõ **đúng số 1** → **Gửi đề xuất**

Gõ số lớn hơn 259 sẽ lại nâng nhu cầu sản xuất — đó chính là cách nó nhảy lên 300.

### Kiểm tra PSX ảo đã lên web chưa

`dist` build lúc 9:08 sáng 29/07, mới hơn mọi tệp nguồn. Mở web thật xem có 5 phiếu
`VIEC-GH`, `VIEC-NH`, `VIEC-DK`, `VIEC-DTNB`, `VIEC-PS` không. Chưa có thì kéo-thả `dist`,
không cần build lại.

### Đổi mật khẩu tài khoản admin

Mật khẩu đã nằm trong lịch sử hội thoại hôm nay.

---

## Bốn điểm phải chốt TRƯỚC khi làm Giai đoạn 2

Chi tiết ở mục 13b của `docs/superpowers/specs/2026-07-29-de-xuat-linh-kien-theo-dot-design.md`.

1. **`get_stock_summary` bỏ sót mã không có dòng tồn kho.** 19 mã **có bán** trong 90 ngày
   không hề xuất hiện trong đầu vào engine — nặng nhất `FK-RO50` bán 165 cái/90 ngày mà
   không bao giờ được đề xuất. Sửa RPC sẽ đụng cột "Cần Bổ Sung" của tab Tồn HH đang chạy.
2. **`retail_qty` / `bom_qty` đụng ý nghĩa cũ** trong bảng `purchase_proposals`.
3. **`missingParams` chưa đủ** — mã bị réo vẫn sinh ra dòng trông bình thường nhưng tính
   theo tham số đã bị kẹp.
4. **Màn hình phải làm tròn khi hiện hiệu số** — trừ bằng float lệch chữ số cuối ở ~3,2%
   số dòng.

---

## Đính chính

Ba con số tồn kho tôi đưa trong lúc bàn thiết kế đều **sai**, do truy vấn của tôi có phép
nối làm nhân bản dòng tồn: F-OCB10 là **959** chứ không phải 6.713; F-CTO10 là **868**
chứ không phải 5.208; OF-QD10 là **748** chứ không phải 18.700. Kết luận thiết kế không
đổi và đã được chứng minh lại bằng dữ liệu thật; số trong tài liệu đã sửa.

Con số 18.705 máy WT-028S-RO là **đúng** — từ truy vấn khác, không dính lỗi này.

---

## Phần chưa kiểm chứng được

Engine JavaScript **chưa từng chạy trên dữ liệu thật**. Khoá công khai bị RLS chặn (đúng
thiết kế), nên bản đối chiếu 100 dòng ở trên được tính bằng cách dựng lại thuật toán bằng
SQL, không phải bằng chính mã JS. Hai thứ kiểm bằng hai đường: mã JS có 40 test đơn vị
kèm kiểm chứng đột biến, SQL có dữ liệu thật. Nối hai đường lại là việc của Giai đoạn 2.

`scripts/doi-chieu-mrp.mjs` chưa tạo, vì lý do trên.

---

## Còn lại

Giai đoạn 2 (bảng đợt + RLS), 3 (màn duyệt nháp), 4 (nối nhập kho) chưa bắt đầu.
Mỗi giai đoạn sẽ có kế hoạch riêng.
